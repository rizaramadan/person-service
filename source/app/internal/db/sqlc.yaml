version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "generated"
        emit_prepared_queries: true
        emit_empty_slices: true

